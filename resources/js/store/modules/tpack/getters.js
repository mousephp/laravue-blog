let getters = {
    tpacks: state => {
        return state.tpacks
    },
    tpack: state => {
        return state.tpack
    },
    message: state => {
        return state.message;
    }
}

export default getters